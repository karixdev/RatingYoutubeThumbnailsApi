spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/youtube-thumbnail-ranking-test
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    username: greenmail-user
    password: greenmail-password
    host: 127.0.0.1
    port: 3025
    test-connection: false
    protocol: smtp


email-verification:
  expiration-hours: 24
  max-number-of-mails-per-hour: 5

email-sender:
  sender: test@youtube-thumbnail-ranking.com

youtube-api:
  base-url: https://youtube.googleapis.com/youtube/v3
  key: api-key